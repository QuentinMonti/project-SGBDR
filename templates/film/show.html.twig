{% extends 'base.html.twig' %}

{% block title %}Film{% endblock %}

{% block body %}
    <h1>Film</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Film_id</th>
                <td>{{ film.filmId }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ film.title }}</td>
            </tr>
            <tr>
                <th>Rental_rate</th>
                <td>{{ film.rentalRate }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ film.rating }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('film_index') }}">back to list</a>

    <a href="{{ path('film_edit', {'film_id': film.film_id}) }}">edit</a>

    {{ include('film/_delete_form.html.twig') }}
{% endblock %}
